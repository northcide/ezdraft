# EasyDraft

A web-based league draft system.

## Stack
- PHP / MySQL (LAMP)
- Vanilla JS / HTML / CSS

## Setup
1. Clone the repo
2. Copy `api/config.example.php` to `api/config.php` and fill in your DB credentials
3. Import `sql/schema.sql` into your MySQL database
4. Point your web server at the project root
